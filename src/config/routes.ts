export enum AuthRoutes {
  auth = 'auth',
  signIn = 'sign-in',
  signUp = 'sing-up',
}

export enum AdminRoutes {
  admin = 'admin',
  posts = 'posts',
  users = 'users',
  add = 'add',
  edit = 'edit',
  show = 'show',
}

export enum Routes {
  home = '/',
}
